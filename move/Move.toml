[package]
name = "data_coop"
edition = "2024.beta"
version = "0.1.0"

[dependencies]
# Sui, MoveStdlib, etc. are auto-added by the toolchain (sui >= 1.45).

[addresses]
data_coop = "0x0"

[dev-dependencies]

[dev-addresses]
